import {Link} from "react-router-dom"; function plain(html){const div=document.createElement("div");div.innerHTML=html;return div.textContent||""} export default function PostCard({post}){const text=plain(post.content);return <article className="post-card"><div className="post-meta">{post.publishStatus} · {new Date(post.createdDate).toLocaleDateString()}</div><h3>{post.title}</h3><p>{text.slice(0,150)}{text.length>150?"…":""}</p><Link className="read-link" to={`/post/${post._id}`}>Read post →</Link></article>}
