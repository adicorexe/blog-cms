import {Link,useNavigate} from "react-router-dom"; import {useAuth} from "../context/AuthContext";
export default function Navbar(){const{isAuthenticated,user,logout}=useAuth();const navigate=useNavigate();const handleLogout=()=>{logout();navigate("/")};return <header className="navbar"><Link className="brand" to="/">Blog CMS</Link><nav><Link to="/">Home</Link>{isAuthenticated?<><Link to="/dashboard">Dashboard</Link><Link to="/editor">New Post</Link><span className="user-chip">{user?.name}</span><button className="link-button" onClick={handleLogout}>Logout</button></>:<><Link to="/login">Login</Link><Link className="nav-cta" to="/register">Get started</Link></>}</nav></header>}
