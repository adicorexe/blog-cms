import {useEffect,useState} from "react"; import {Link} from "react-router-dom"; import {apiRequest} from "../services/api"; import PostCard from "../components/PostCard";
export default function Home(){const[posts,setPosts]=useState([]);const[loading,setLoading]=useState(true);useEffect(()=>{apiRequest("/posts").then(setPosts).catch(()=>setPosts([])).finally(()=>setLoading(false))},[]);return <><section className="hero"><div><span className="eyebrow">CONTENT MANAGEMENT SYSTEM</span><h1>Write. Build.<br/><em>Publish.</em></h1><p>A focused workspace for creating polished blog content with a rich editor and flexible page builder.</p><Link className="primary-btn" to="/register">Start writing</Link></div><div className="hero-card"><div className="mini-top">EDITOR PREVIEW <span>●</span></div><div className="mini-title">A better place<br/>for your ideas.</div><div className="mini-lines"><i/><i/><i/></div></div></section><section className="section-head"><div><span className="eyebrow">LATEST STORIES</span><h2>Published posts</h2></div></section>{loading?<p className="muted">Loading posts…</p>:posts.length?<div className="post-grid">{posts.map(post=><PostCard key={post._id} post={post}/>)}</div>:<div className="empty-state"><h3>No published posts yet</h3><p>Create an account and publish your first article.</p></div>}</>}
