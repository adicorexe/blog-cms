import {useEffect,useState} from "react"; import {Link,useParams} from "react-router-dom"; import {apiRequest} from "../services/api";
export default function Post(){const{id}=useParams();const[post,setPost]=useState(null);const[error,setError]=useState("");useEffect(()=>{apiRequest(`/posts/${id}`).then(setPost).catch(err=>setError(err.message))},[id]);if(error)return <div className="empty-state"><h2>{error}</h2><Link to="/">Back home</Link></div>;if(!post)return <p className="muted">Loading…</p>;return <article className="single-post"><Link to="/" className="back-link">← All posts</Link><div className="post-meta">Published {new Date(post.createdDate).toLocaleDateString()} · By {post.author?.name}</div><h1>{post.title}</h1><div className="post-content" dangerouslySetInnerHTML={{__html:post.content}}/></article>}
